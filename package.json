{
  "name": "readnwrite",
  "version": "1.0.0",
  "description": "A complex web application for reading and writing content",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm install",
    "test": "jest",
    "lint": "eslint ."
  },
  "keywords": [
    "read",
    "write",
    "content",
    "web-app"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
